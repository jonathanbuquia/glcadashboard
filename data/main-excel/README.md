# Main Excel Workbook

This folder stores the primary Excel workbook that will be used as the app's data source.

Current workbook:

- `Sample Dashboard.xlsx`

Keep only the active workbook here unless we intentionally add examples, exports, or archived versions later.
