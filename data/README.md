# Data

This folder holds source files used to build or feed the app.

- `main-excel/` contains the primary Excel workbook.

Keep raw source files here and app code in `src/`. Add processing scripts only when the app needs a repeatable export step.
